/**
 * Copy to preview-auth.config.ts and set the preview@pinchthis.com Firebase Auth password.
 * preview-auth.config.ts is gitignored and must not be committed.
 */
export const PREVIEW_AUTH = {
  password: 'REPLACE_WITH_PREVIEW_PASSWORD',
};
