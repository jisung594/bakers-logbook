/** @type {import('tailwindcss').Config} */
module.exports = {
  // Below tells Tailwind where to scan for classes (HTML templates and TS component files)
  content: [
    "./src/**/*.{html,ts}", 
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}
